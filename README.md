int max(const int* arr, int size) {
    
    int maxVal = arr[0];
    for (int i = 1; i < size; i++) {
        if (arr[i] > maxVal) {
            maxVal = arr[i];
        }
    }
    return maxVal;
}

Max - функция для нахождения максимума.

Она принимает:
*arr - указатель на массив.
size - количество элементов в массиве.

Maxval - максимальное значение, в начале присвоим первый элеиент массива.

Далее идёт поиск максимума: если текущий элемент больше максимального значения, то присваеваем максимальному значению значение данного элемента.

В конце функция возвращает найденный максимум.


